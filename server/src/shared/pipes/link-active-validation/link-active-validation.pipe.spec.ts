import { LinkActiveValidationPipe } from './link-active-validation.pipe';

describe('LinkActiveValidationPipe', () => {
  it('should be defined', () => {
    expect(new LinkActiveValidationPipe()).toBeDefined();
  });
});
